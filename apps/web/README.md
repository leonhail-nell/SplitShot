# @splitshot/web

Next.js App Router — API, Auth.js, AI parse, history, Stripe settle-up.

Run from monorepo root:

```bash
npm run dev
```

Or:

```bash
npm run dev -w @splitshot/web
```

Env file: `.env` in this directory (see `.env.example`).
